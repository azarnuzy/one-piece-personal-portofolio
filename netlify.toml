[build]
command = "pnpm build && mkdir -p .output/public"
publish = ".output/public"

[build.environment]
NITRO_PRESET = "netlify"
NODE_VERSION = "20"
SKIP_ENV_VALIDATION = "true"

[[redirects]]
from = "/*"
to = "/.netlify/functions/server"
status = 200
